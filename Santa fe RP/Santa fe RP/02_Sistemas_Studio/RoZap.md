# RoZap (client) — Mensagens
- Path: `StarterPlayer.StarterPlayerScripts.Client.GuiHandler.Celular.RoZap`.
- Modo novo (CelularV2): lista por foto+nome (número interno), conversa carrega histórico, envia/recebe ao vivo, "+" = adicionar contato (lista de donos, mesmo offline). Legado name-based quando chave off.
- ~330 linhas (refatorar em módulos quando mexer).
